{
  "name": "@brandora/web",
  "version": "0.1.0",
  "private": true,
  "description": "Brandora — static front end. Data and locales are generated from the TypeScript packages.",
  "scripts": {
    "dev": "python3 -m http.server 4100",
    "build": "echo 'static site — data is emitted by the engine packages' && exit 0",
    "test": "node ../../scripts/check-brandora.mjs"
  }
}
