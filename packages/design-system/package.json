{
  "name": "@harmony/design-system",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Canonical Harmony Verify design tokens, shared by every app and the static marketing site",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./tokens.css": "./dist/tokens.css"
  },
  "scripts": {
    "build": "node --experimental-strip-types scripts/sync-tokens.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
